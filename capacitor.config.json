import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voice.app',
  appName: 'VoiceChat',
  webDir: 'www',
  server: {
    url: 'https://crconferensimessenger.vercel.app/'
    // cleartext: false  ← удали эту строку, она не нужна для HTTPS
  }
};

export default config;
