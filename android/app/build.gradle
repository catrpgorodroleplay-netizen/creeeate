apply plugin: 'com.android.application'

android {
    namespace "com.voice.app"
    compileSdk 35
    defaultConfig {
        applicationId "com.voice.app"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

repositories {
    google()
    mavenCentral()
    maven { url = "https://jitpack.io" }  // Добавляем JitPack для ZeroTier
    maven { url = "https://maven.zerotier.com/repository/maven-releases" }  // Официальный репозиторий ZeroTier
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.9.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    
    // Capacitor
    implementation project(':capacitor-android')
    implementation project(':capacitor-cordova-android-plugins')
    
    // ZeroTier (пробуем разные варианты)
    implementation 'com.zerotier:libzt:1.12.2'              // официальная версия
    // Если не сработает — попробуй:
    // implementation 'com.github.zerotier:libzt:1.12.2'
    
    // Foreground Service для микрофона
    implementation 'androidx.core:core:1.12.0'
}
